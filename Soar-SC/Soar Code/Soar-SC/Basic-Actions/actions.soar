source gather-minerals.soar
source wait.soar
source build-units.soar
source build-supply-depots.soar
source build-barracks.soar
source generic-build-operators.soar

#TODO: Add support for a build-unit operator and a build-building operator to make things more general and only have to propose building the building and it will apply it (less specifics for each building)

sp {remove*output-commands
   (state <s> ^name Soar-SC
              ^operator <op>
              ^io.output-link <o>)
   (<o> ^<attr> <object>)
   (<object> ^status complete)
-->
   (<o> ^<attr> <object> -)
   (write |remove*output-commands| (crlf))
}
