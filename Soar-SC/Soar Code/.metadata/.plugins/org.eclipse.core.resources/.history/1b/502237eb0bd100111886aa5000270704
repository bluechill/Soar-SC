sp {propose*wait
   (state <s> ^name Soar-SC)
-->
   (<s> ^operator <o> + <)
   (<o> ^name wait)
}

sp {apply*wait
   (state <s> ^operator.name wait
              ^io.output-link <o>)
-->
   (<o> ^wait <wait>)
}